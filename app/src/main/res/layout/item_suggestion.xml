<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:minHeight="60dp"
    android:layout_marginHorizontal="12dp"
    android:layout_marginTop="2dp"
    android:background="@drawable/bg_group_row"
    android:clickable="true"
    android:focusable="true"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingHorizontal="16dp"
    android:paddingVertical="8dp">

    <FrameLayout
        android:layout_width="40dp"
        android:layout_height="40dp">

        <TextView
            android:id="@+id/avatarInitial"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/bg_circle_solid"
            android:gravity="center"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:textStyle="bold"
            android:visibility="gone"
            tools:text="Y"
            tools:visibility="visible" />

        <com.google.android.material.imageview.ShapeableImageView
            android:id="@+id/photo"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:visibility="gone"
            app:shapeAppearanceOverlay="@style/CircleImageView" />
    </FrameLayout>

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="14dp"
        android:orientation="vertical">

        <LinearLayout
            android:id="@+id/quickDialBadge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="2dp"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:visibility="gone"
            tools:visibility="visible">

            <ImageView
                android:layout_width="12dp"
                android:layout_height="12dp"
                android:src="@drawable/ic_bolt"
                android:importantForAccessibility="no"
                app:tint="?attr/colorPrimary" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="3dp"
                android:text="@string/quick_dial_badge"
                android:textColor="?attr/colorPrimary"
                android:textSize="11sp"
                android:textStyle="bold" />
        </LinearLayout>

        <TextView
            android:id="@+id/name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:maxLines="1"
            android:ellipsize="end"
            android:textAppearance="?attr/textAppearanceBodyLarge"
            tools:text="Kol Mevaser" />

        <TextView
            android:id="@+id/number"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:maxLines="1"
            android:ellipsize="end"
            android:textAppearance="?attr/textAppearanceBodyMedium"
            android:textColor="?attr/colorOnSurfaceVariant"
            tools:text="(845) 351-1200" />
    </LinearLayout>

    <!-- Message shortcut: the whole trailing corner of the row is the tap
         target (not just the icon glyph) — the icon glyph used to sit in a
         40dp circle with the rest of the row still wired to Call, so a tap
         landing just outside that circle (but still visually "on" the icon
         area) fired a call instead of a text. layout_height=match_parent +
         scaleType=center keeps the icon glyph the same visual size while
         growing the actual touchable/clickable bounds around it. Touch-only
         by design (focusable=false): D-pad should skip straight past it. -->
    <ImageView
        android:id="@+id/btnMessage"
        android:layout_width="64dp"
        android:layout_height="match_parent"
        android:layout_marginStart="8dp"
        android:scaleType="center"
        android:background="?attr/selectableItemBackground"
        android:src="@drawable/ic_message"
        android:contentDescription="@string/opt_message"
        app:tint="?attr/colorOnSurfaceVariant"
        android:clickable="true"
        android:focusable="false"
        android:focusableInTouchMode="false" />

</LinearLayout>
